<Window
    x:Class="LAMG.UI.Views.Dialogs.ReusePoolSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:LAMG.UI.ViewModels.Dialogs"
    d:DataContext="{d:DesignInstance Type=vm:ReusePoolSelectionDialogViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Select reuse-pool batches"
    Height="500"
    Width="640"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Choose which batches feed the reuse-mode pool. Unchecked batches will not contribute tracks. Cancel skips reuse-mode entirely."
            FontWeight="SemiBold"
            TextWrapping="Wrap"
            Margin="0,0,0,12" />

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Margin="0,0,0,8">
            <Button
                Content="Select all"
                Click="OnSelectAll"
                MinWidth="90"
                Margin="0,0,8,0" />
            <Button
                Content="Select none"
                Click="OnSelectNone"
                MinWidth="90" />
        </StackPanel>

        <DataGrid
            Grid.Row="2"
            ItemsSource="{Binding Batches}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            GridLinesVisibility="Horizontal"
            RowHeight="26">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn
                    Header="Include"
                    Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                    Width="80" />
                <DataGridTextColumn
                    Header="Folder"
                    Binding="{Binding SourceFolder}"
                    Width="*"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Header="Tracks"
                    Binding="{Binding TrackCount}"
                    Width="80"
                    IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,12,0,0">
            <Button
                Content="Cancel"
                Click="OnCancel"
                IsCancel="True"
                MinWidth="90"
                Margin="0,0,8,0" />
            <Button
                Content="Use selected"
                Click="OnAccept"
                IsDefault="True"
                FontWeight="SemiBold"
                MinWidth="120" />
        </StackPanel>
    </Grid>
</Window>
